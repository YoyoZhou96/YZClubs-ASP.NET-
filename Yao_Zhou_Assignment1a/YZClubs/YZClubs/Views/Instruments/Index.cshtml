@model IEnumerable<YZClubs.Models.Instrument>

@{
    ViewData["Title"] = "List of all Instruments";
}

<h1>List of all Instruments</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="table_id">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.InstrumentId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.InstrumentId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.InstrumentId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<script src="~/lib/jquery/dist/jquery.min.js"></script>

<script>
    $(document).ready(function () {
        $('#table_id').DataTable();
    });
</script>

